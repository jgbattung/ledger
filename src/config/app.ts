/**
 * Single source of truth for the app name. Every place the app name appears
 * (document title, PWA manifest name/short_name, in-app UI) must import this
 * constant rather than hardcoding the string.
 */
export const APP_NAME = 'Ledger'
